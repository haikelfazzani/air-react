<!---Badge.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Modal from '../src/modal/index'
import '../src/modal/index.css'

<Meta title="Modal" component={Modal} />

export const Template = (args) => <Modal>{args.children}</Modal>

# Modal

The Modal component is used to trigger an action or event, such as submitting a form, opening a dialog, canceling an action, or performing a delete operation.

<Canvas>
  <Story 
    name="modal"
    args={{
      children:<div>Hello world</div>
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
